import 'package:flutter/material.dart';

class Esewa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Esewa"),
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            Text("esewa sdk integration"),
          ],
        ),
      ),
    );
  }
}
